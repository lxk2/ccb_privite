const SUCCESS_CODE = 1

const ERROR_CODE = 0

const ERR_AUTH_CODE = -1

const DOMAIN = 'http://www.tcm.top'

const BASE_URL = DOMAIN + '/api/v1.'

const UPLOAD_URL = ''

export default {
  SUCCESS_CODE,
  ERROR_CODE,
  ERR_AUTH_CODE,
  DOMAIN,
  BASE_URL,
  UPLOAD_URL
}
